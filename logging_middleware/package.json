{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "Reusable logging middleware that posts structured logs to the evaluation /logs API with Bearer auth and automatic token refresh.",
  "main": "src/index.js",
  "scripts": {
    "test:smoke": "node examples/basic.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
